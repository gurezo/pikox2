#include "mbed.h"

DigitalOut myled(LED1);
DigitalIn TouchSensor(p5);

int main() {
    while(1) {
        if(TouchSensor==1){
            myled = 1;
        } else {
            myled = 0;
        }
        wait(0.2);
    }
}
